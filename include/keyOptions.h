#ifndef KEY_OPTIONS
#define KEY_OPTIONS

#include <string>

class KeyOptions
{
public:
   void keyComapare();
   void generateRandomKey();
   void generateBaseCodeKey(const unsigned short& baseCode);
};

#endif