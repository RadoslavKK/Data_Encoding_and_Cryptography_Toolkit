#ifndef KEY_OPTIONS
#define KEY_OPTIONS

#include <string>

class KeyOptions
{
public:
   void keyComapare();
   void generateRandomKey();
   void generateBaseCodeKey(unsigned short baseCode);
};

#endif