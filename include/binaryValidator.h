#ifndef BINARY_VALIDATOR_H
#define BINARY_VALIDATOR_H

#include <string>

// Checks if the given string contains only binary characters (0 or 1).
bool isValidBinary(const std::string &binaryString);

#endif